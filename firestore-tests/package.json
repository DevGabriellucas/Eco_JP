{
  "name": "ecojp-firestore-rules-tests",
  "private": true,
  "description": "Testes automatizados das regras do Firestore (firestore.rules), rodando contra o emulador local. Não faz parte do app Flutter — cobre a camada de segurança que o app inteiro depende.",
  "scripts": {
    "pretest": "node -e \"require('fs').copyFileSync('../firestore.rules', 'firestore.rules')\"",
    "test": "firebase emulators:exec --only firestore --project ecojp-rules-test \"mocha --timeout 20000 rules.test.mjs\""
  },
  "type": "module",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "mocha": "^10.7.3"
  }
}
