{
  "name": "ecojp-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Testes das regras de seguranca do Firestore do EcoJP (emulador).",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"jest --runInBand\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "jest": "^29.7.0"
  }
}
